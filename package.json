{
  "name": "minimos",
  "version": "1.0.0",
  "description": "lightweight ui framework",
  "main": "index.js",
  "scripts": {
    "build": "yarn clean && mkdir dist/ && cp index.html dist/ && cp css dist/ && cp js dist/",
    "clean": "rimraf dist/",
    "format": "yarn prettier --write .",
    "format-check": "yarn prettier --check .",
    "validate": "yarn format-check"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
