{
  "name": "minimos",
  "version": "1.0.0",
  "description": "lightweight ui framework",
  "main": "index.js",
  "scripts": {
    "build": "yarn clean && mkdir dist/ && cp src/*.html dist/ && cp -r src/css dist/ && cp -r src/js dist/",
    "clean": "rimraf dist/",
    "format": "yarn prettier --write .",
    "format-check": "yarn prettier --check .",
    "validate": "yarn format-check"
  },
  "author": "dylanized",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
