{
  "name": "minimos",
  "version": "1.0.0",
  "description": "simple ui framework",
  "main": "index.js",
  "scripts": {
    "build": ". bin/build",
    "clean": "rimraf dist/",
    "dev": "yarn start",
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "format-dist": "prettier --log-level silent --write dist/",
    "render": ". bin/render",
    "start": "node server.js",
    "start-dist": "node server-static.js",
    "validate": "yarn format-check"
  },
  "author": "dylanized",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  }
}
